search_id: tcn_epochs_lr_v1
# This must be the immutable 25k baseline that actually produced the banks
# used by the incremental gates, not the pre-repair v1 preparation config.
base_config: configs/reference_framework_v1/experiments/post_ny_epoch_stack_candidate_v2.yaml
target_model: tcn
stage: screen
candidates:
  - id: post_ny_screen_tcn_h180_e2_lr1e4_v1
    overrides:
      models:
        tcn:
          history_days: 180
          base: {epochs: 2, learning_rate: 0.0001, scheduler: cosine, warmup_fraction: 0.10, weight_decay: 0.0001}
  - id: post_ny_screen_tcn_h180_e2_lr3e4_v1
    overrides:
      models:
        tcn:
          history_days: 180
          base: {epochs: 2, learning_rate: 0.0003, scheduler: cosine, warmup_fraction: 0.10, weight_decay: 0.0001}
  - id: post_ny_screen_tcn_h180_e2_lr8e4_v1
    overrides:
      models:
        tcn:
          history_days: 180
          base: {epochs: 2, learning_rate: 0.0008, scheduler: cosine, warmup_fraction: 0.10, weight_decay: 0.0001}
