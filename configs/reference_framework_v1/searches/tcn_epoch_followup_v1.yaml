search_id: tcn_epoch_followup_v1
# Follow-up only after the broad two-epoch LR bracket.  Keep the central,
# non-harmful LR and alter no model other than TCN.
base_config: configs/reference_framework_v1/experiments/post_ny_epoch_stack_candidate_v2.yaml
target_model: tcn
stage: screen
candidates:
  - id: post_ny_screen_tcn_h180_e1_lr3e4_v1
    overrides:
      models:
        tcn:
          history_days: 180
          base: {epochs: 1, learning_rate: 0.0003, scheduler: cosine, warmup_fraction: 0.10, weight_decay: 0.0001}
  - id: post_ny_screen_tcn_h180_e3_lr3e4_v1
    overrides:
      models:
        tcn:
          history_days: 180
          base: {epochs: 3, learning_rate: 0.0003, scheduler: cosine, warmup_fraction: 0.10, weight_decay: 0.0001}
