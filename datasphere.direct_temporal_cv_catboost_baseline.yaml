name: ozon-direct-temporal-cv-catboost-baseline
desc: Four-fold 250k direct CatBoost temporal validation with the external baseline recipe.
cmd: >
  export PYTHONPATH=. ; python3 -u scripts/run_direct_temporal_cv.py
  --experiment-config configs/direct_temporal_cv_v1/baseline_catboost.yaml
  --pre-run-sha __PRE_RUN_SHA__
env:
  python:
    type: manual
    version: 3.10.13
    requirements-file: requirements-datasphere.txt
    local-paths:
      - src/
      - scripts/
      - configs/direct_temporal_cv_v1/
      - requirements-datasphere.txt
      - data/train.parquet
      - sample_submit.csv
outputs:
  - artifacts/direct_temporal_cv_v1/experiments/direct_cv_catboost_baseline_v1/
cloud-instance-types:
  - g1.1
working-storage:
  type: SSD
  size: 100Gb
graceful-shutdown:
  signal: SIGTERM
  timeout: 30s
