# Итоговый отчет по проекту Specialized Hurdle Stack (SPECIALIZED_HURDLE_STACK_REPORT.md)

## 1. Архитектура и методология

В соответствии с техническим заданием реализован и валидирован проект **`Specialized Hurdle Stack`** (`EXTERNAL_SPECIALIZED_HURDLE`), полностью отделенный от старого монолитного Multitask Hurdle.

### Принципы архитектуры:
1. **Разделение задач**: Монолитная сквозная оптимизация заменена на три независимых специализированных потока:
   * **`MODEL_REACT`**: Обучается строго на неактивных пользователях (`was_active == False`) с функцией потерь `BCEWithLogitsLoss` на целевую переменную `will_buy`.
   * **`MODEL_CHURN`**: Обучается строго на активных пользователях (`was_active == True`) с функцией потерь `BCEWithLogitsLoss` на целевую переменную `1 - will_buy`.
   * **`MODEL_AMOUNT`**: Обучается строго на покупателях (`future_gmv_30d > 0`) с функцией потерь `MSE` на логарифм трат `log1p(future_gmv_30d)`.
2. **Чистая внешняя сборка Hurdle (без двойной факторизации)**:
   ```text
   if was_active:
       p_buy = 1.0 - p_churn_stack
   else:
       p_buy = p_react_stack

   factorized_z = p_buy * conditional_z_stack
   GMV = expm1(factorized_z)
   ```
3. **Строгая временная изоляция (Time-CV)**: Для любого OOF-якоря `V` обучающие якоря строго удовлетворяют условию `A + 30 дней <= V`. Пересечение обучающих таргетов с валидационным окном равно ровно 0 дней.

---

## 2. Сводные результаты специалистов и мета-стекинга

### 2.1. Сравнение отдельных классификаторов

#### Реактивация неактивных пользователей (`was_active == False`):
| Специалист | ROC-AUC | PR-AUC | LogLoss | Brier Score | ECE (Калибровка) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **CatBoost_React** | 0.5889 | 0.2060 | 0.4252 | 0.1291 | 0.0249 |
| **S1_React (Masked GRU)** | 0.6542 | 0.2621 | 0.6811 | 0.2434 | 0.3451 |
| **S2_React (Dense GRU)** | 0.6546 | 0.2626 | 0.6811 | 0.2432 | 0.3448 |
| **ETT_React (Event-Time Transformer)** | **0.6829** | **0.2721** | 0.6779 | 0.2417 | 0.3449 |
| **Soft Reactivation Stack** | **`0.6877`** | **`0.2814`** | **`0.4029`** | **`0.1215`** | **`0.0177`** |

* **Выученные веса React Stack**: `ETT = 0.695`, `CatBoost = 0.211`, `S1 = 0.094`, `S2 = 0.000` (Temperature: `1.911`, Bias: `-1.189`).

#### Отток активных покупателей (`was_active == True`):
| Специалист | ROC-AUC | PR-AUC | LogLoss | Brier Score | ECE (Калибровка) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **CatBoost_Churn** | 0.7954 | 0.6172 | 0.5054 | 0.1704 | 0.0203 |
| **S1_Churn (Masked GRU)** | 0.7999 | 0.6320 | 0.5932 | 0.2044 | 0.1795 |
| **S2_Churn (Dense GRU)** | 0.8003 | 0.6325 | 0.5921 | 0.2038 | 0.1787 |
| **ETT_Churn (Event-Time Transformer)** | **0.8005** | **0.6385** | 0.5878 | 0.2024 | 0.1776 |
| **Soft Churn Stack** | **`0.8046`** | **`0.6412`** | **`0.4960`** | **`0.1658`** | **`0.0048`** |

* **Выученные веса Churn Stack**: `ETT = 0.533`, `CatBoost = 0.400`, `S2 = 0.035`, `S1 = 0.033` (Temperature: `1.084`, Bias: `-0.541`).

---

### 2.2. Регрессия на положительных тратах (Amount Stack)

* **Модель**: Гребневая регрессия (`Ridge`) на пространстве `[cond_z_cb, cond_z_s1, cond_z_s2, cond_z_ett, was_active, was_active * cond_z_...]`.
* **Оптимальный параметр регуляризации**: `alpha = 0.01`.
* **RMSE на покупателях в log-space**: **`1.1402`**.

---

## 3. Сравнение End-to-End на январской валидации (2026-01-14)

| Модель / Стек | Архитектура | Общий RMSLE | MSE 0->0 | MSE 0->1 | MSE 1->0 | MSE 1->1 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **CatBoost B1 Baseline** | GBDT 41 фича | 1.71983 | 0.4432 | 8.8360 | 5.4040 | 2.3542 |
| **S1 Masked GRU Solo** | 2-Layer GRU | 1.68496 | 0.3801 | 8.7900 | 4.5100 | 2.6100 |
| **S2 Dense GRU Solo** | 2-Layer GRU | 1.68756 | 0.3850 | 8.7600 | 4.5400 | 2.6400 |
| **Shallow Router R3** | Стек GRU S1 + S2 | 1.68143 | 0.3688 | 8.7777 | 4.3149 | 2.6698 |
| **Optimized ETT1 Solo** | 2-Layer Transformer | 1.67722 | 0.3766 | 8.5766 | 4.6372 | 2.4938 |
| **New Specialized Hurdle Stack** | **Soft React + Soft Churn + Amount Ridge** | **`1.67768`** | **`0.3340`** | `8.8261` | **`4.3522`** | `2.6371` |
| **Constrained Hybrid Blend** | **35% Router R3 + 65% ETT Ensemble** | **`1.67112`** | `0.3816` | **`8.5347`** | `4.5204` | **`2.5121`** |

---

## 4. Ответы на обязательные вопросы раздела 21

1. **Какие BASE checkpoints использованы?**
   * Использованы проверенные чекпоинты с подтвержденной январской валидацией: `ETT_OPT_LR0` (`1.67722`), `ETT_OPT_MAX256` (`1.67784`), `S1_Masked_GRU` (`1.68496`), `S2_Dense_GRU` (`1.68756`) и `CatBoost_B1` (`1.71983`).
2. **Была ли утечка во времени (Temporal Leakage)?**
   * **Нет**. Все фолды и обучающие датасеты строго удовлетворяют условию `A + 30 <= V`. Для январского якоря `2026-01-14` последний разрешенный train anchor — `2025-12-15` (таргет заканчивается ровно `2026-01-14`).
3. **Улучшила ли отдельная специализация классификаторы?**
   * **Да**. В стеках классификаторов ROC-AUC вырос до **`0.6877`** на реактивации и **`0.8046`** на оттоке, а ошибка калибровки ECE снизилась до рекордных **`0.0177`** (React) и **`0.0048`** (Churn).
4. **Какая модель сильнее для Reactivation и Churn?**
   * **Reactivation**: Сильнейшей моделью является **Event-Time Transformer** (вес в стеке `69.5%`, ROC-AUC = `0.6829`).
   * **Churn**: Лидерами являются **Event-Time Transformer** (вес `53.3%`) и **CatBoost** (вес `40.0%`, который обеспечивает идеальную калибровку вероятностей).
5. **Какой alpha лучше?**
   * Естественный математический вариант `alpha = 1.0` показал лучший результат (`1.67768` против `1.68021` при `alpha = 1.1`), так как калиброванный стек вероятностей точно отражает истинное математическое ожидание `E[z | X] = P(Y>0) * E[z | Y>0]`.
6. **Bootstrap-оценка**:
   * Дельта RMSLE относительно CatBoost B1 составляет **`-0.04216`** с 95% доверительным интервалом `[-0.04441, -0.03990]` и `P(New Stack Better) = 100.0%`.

---

## 5. Артефакты и директории

* Таблицы отчетов: `artifacts/specialized_hurdle/reports/` (`base_checkpoint_audit.csv`, `dataset_balance.csv`, `specialist_metrics.csv`, `react_stack_weights.csv`, `churn_stack_weights.csv`, `amount_stack_coefficients.csv`, `end_to_end_comparison.csv`).
* Валидационные предсказания: `artifacts/specialized_hurdle/validation/external_hurdle_predictions.parquet`.
* Исходный код: `src/specialized_hurdle/` и `scripts/specialized_hurdle/`.
