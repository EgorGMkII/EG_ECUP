# Отчет о воспроизведении и аудите T5 Simplified Transformer (Этап 0 и Этап A)

## 1. Введение и цели исследования

Цель этапов 0 и A — провести строгий, воспроизводимый и математически честный аудит ранее заявленных результатов **T5 Simplified Transformer** (`1.66866`), исключив любые потенциальные утечки будущего таргета, исследовав влияние крайней границы обучающих якорей (`ANCHORS_V2_EDGE`), а также протестировав стабильность архитектуры на очищенной временной валидации.

---

## 2. Этап 0: Аудит крайнего якоря (`ANCHORS_V1` vs `ANCHORS_V2_EDGE`)

### Определение временных окон:
* **Target window**: `[anchor + 1 day, anchor + 30 days]`.
* **Валидационный якорь**: `2026-01-14` (таргет: `2026-01-15 .. 2026-02-13`).
* **`ANCHORS_V1` (11 якорей)**: последний якорь `2025-12-08` (таргет заканчивается `2026-01-07`, зазор до валидации = 7 дней, overlap = 0 дней).
* **`ANCHORS_V2_EDGE` (12 якорей)**: добавлен максимально поздний честный якорь `2025-12-15` (таргет длится с `2025-12-16` по `2026-01-14`). Зазор до валидации = 0 дней, **пересечение строго равно 0 дней**.

### Результаты контрольного эксперимента (Каноническая GRU-180):

| Модель | Набор якорей | Кол-во якорей | RMSLE (Val 2026-01-14) | Delta vs V1 | 0->0 MSE | 0->1 MSE | 1->0 MSE | 1->1 MSE |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **`A0_V1`** | `ANCHORS_V1` | 11 | **1.68846** | 0.00000 | 1.0655 | 6.0821 | 6.4158 | 2.0175 |
| **`A0_V2`** | `ANCHORS_V2_EDGE` | 12 | **1.68887** | +0.00041 | 1.0135 | 6.1989 | 6.4670 | 2.0083 |

**Вывод по Этапу 0**:
Добавление якоря `2025-12-15` дает практически эквивалентный результат (разница в пределах шума $\pm 0.0004$), подтверждая корректность границ, но не создавая искусственного искажения метрик.

---

## 3. Этап A1: Аудит исторического происхождения T5 (`1.66866`)

В ходе аудита исходных скриптов (`scratch/audit_transformer_part4_rehab_suite.py`) и логов было установлено:
1. **Факт утечки**: В историческом коде обучение запускалось на срезе `anchors[-6:]`, куда входили `2025-12-22`, `2026-01-05` и сам валидационный якорь `2026-01-14`. Таргеты этих дат напрямую перекрывали целевое окно валидации `2026-01-15 .. 2026-02-13`.
2. **Статус артефактов**: На диске отсутствовали сохраненные веса и предсказания (`UNVERIFIED_REPORT_ONLY`).
3. **Итоговый вердикт**: Результат `1.66866` признан **недействительным из-за прямого target leakage** и аннулирован в соответствии со стандартами честной валидации.

Результаты аудита зафиксированы в [`artifacts/t5_reproduction/t5_provenance.json`](file:///c:/Users/egorg/Documents/OZON_ECUP/artifacts/t5_reproduction/t5_provenance.json).

---

## 4. Этап A: Результаты воспроизведения T5 на Purged данных

### Архитектура воспроизведенного T5:
* **Входные данные**: 364 дня (52 патча по 7 дней, с сохранением самого свежего дня `x[:, -364:, :]`).
* **Модель**: 2 слоя PreLN Transformer Encoder, `d_model=128`, `nhead=4`, `dim_feedforward=256`, `dropout=0.20`, temporal attention pooling + 5 transition hurdle heads.
* **Маскирование**: `src_key_padding_mask` по 12 поведенческим признакам + learned `EMPTY_HISTORY` token.

### Таблица результатов T5:

| Эксперимент | Описание | RMSLE | Delta vs GRU A0 | 0->0 MSE | 0->1 MSE | 1->0 MSE | 1->1 MSE |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **`A0_V1` (Baseline)** | Каноническая GRU-180 | **1.68846** | 0.00000 | 1.0655 | 6.0821 | 6.4158 | 2.0175 |
| **`T5_R0`** | T5 на `ANCHORS_V1` (Seed 42) | **1.94259** | +0.25413 | 0.5900 | 7.9480 | 1.7966 | 5.6409 |
| **`T5_R1`** | T5 на `ANCHORS_V2_EDGE` (Seed 42) | **2.11395** | +0.42549 | 0.1609 | 10.5763 | 1.2419 | 7.0518 |
| **`T5_R2_seed43`** | T5 на V1 (Seed 43) | **2.01911** | +0.33065 | 0.4120 | 8.8102 | 1.8410 | 5.8210 |
| **`T5_R2_seed44`** | T5 на V1 (Seed 44) | **1.95091** | +0.26245 | 0.3815 | 8.6514 | 1.7612 | 5.7104 |
| **`T5_R2_3Seed_Ensemble`** | 3-Seed Ансамбль T5 | **1.95598** | +0.26752 | **0.3522** | **9.0191** | **1.7083** | **5.6621** |

---

## 5. Анализ причин поведения T5 на разреженных данных

1. **Коллапс на активных покупателях (Over-Suppression)**:
   * На чистых 364 днях более 80% 7-дневных патчей являются полностью пустыми.
   * Стандартный Transformer с PreLN при глобальном Self-Attention сильно штрафует редкие активные токены на фоне огромного количества нулей.
   * Модель сваливается в тривиальное решение: она идеально подавляет нули для спящих пользователей (`0->0 MSE` упал с 1.0655 до **0.3522**), но практически перестает различать реальных покупателей (`1->1 MSE` взорвался с 2.0175 до **5.6621**, а `0->1 MSE` вырос с 6.0821 до **9.0191**).

2. **Parity Check (`parity_check.json`)**:
   * Максимальные расхождения между старыми нечестными и новыми очищенными предсказаниями составили:
     * `Logits Diff`: **2.08**
     * `Conditional Z Diff`: **15.03**
     * `Embedding Diff`: **9.59**
   * Паритет равен `False`, что математически подтверждает: очищенная модель обучается совершенно иным репрезентациям, нежели утекшая версия.

3. **Аудит тестового распределения (`test_distribution_audit.json`)**:
   * Средний чек на тесте: `9.48 руб.` (P50 = `0.41 руб.`, P99 = `211.72 руб.`).
   * Доля предсказанных нулей: `64.86%` (соответствует реальному уровню активности).

---

## 6. Логарифмический блендинг (Ансамблирование)

Благодаря тому, что T5 имеет совершенно другую структуру ошибок (минимальная дисперсия на нулях, слабая способность в хвосте), его добавление с малым весом к GRU S1 дает **положительный синергетический эффект**:

* **Pure GRU S1 (SSL Masked)**: `RMSLE = 1.68496`
* **Optimal Blend `0.89 * S1 + 0.11 * T5`**: **`RMSLE = 1.68034`** (**-0.00462** прирост по сравнению с Solo S1).

---

## 7. Итоговые выводы и переход к следующим этапам

1. **Вопрос о T5 закрыт**:
   * В качестве соло-модели стандартный Patch-based Transformer (`T5`) на разреженных данных непригоден (`RMSLE ~ 1.95 - 2.11`) без специальной адаптации времени.
   * Исторический `1.66866` был артефактом утечки.
2. **Дальнейшие шаги**:
   * **Этап B**: Построение честного Router между S1 Masked и S2 Dense.
   * **Этап C**: Переход к **Event-Time Transformer** (где токенами являются только реальные активные дни, а время моделируется через аддитивный continuous-time vector + rank embedding).
