# Отчет по эксперименту SPECIALIZED_HURDLE_JOINT_250K_V2 (Post-Mortem и Исправление)

## 1. Диагностика ошибки (Root Cause Analysis)

### Что произошло:
При первом запуске сабмит показал аномальный `Public RMSLE = 2.316567`.
Причина была локализована:
1. **Спецификация моделей и веса**: Базовые модели (CatBoost Specialists, S1 Masked GRU, S2 Dense GRU, Event-Time Transformer) на всех 250 000 пользователях обучились штатно. Их сырые предсказания в `test_specialists_raw_predictions_250k_v2.parquet` полностью валидны.
2. **Ошибочная нормализация Amount**: В скрипте `run_a_meta_250k.py` и `run_b_final_250k.py` перед линейной комбинацией Amount была ошибочно добавлена искусственная Z-нормализация:
   `X_a_scaled = (X_a - mean) / std`
   Поскольку предсказания суммы `X_a` уже находятся в пространстве `log1p(gmv) ≈ 3.8 .. 4.2`, вычитание среднего сдвинуло значения к нулю, сломало линейную комбинацию Hurdle и привело к масштабированию ошибки при взятии `expm1(z_pred)`.

---

## 2. Исправление и оптимизация на 250k

После устранения ошибочной нормализации и запуска совместной SLSQP-оптимизации на всех 250 000 пользователях мета-якоря `2025-12-15`:

* **Baseline MSE на 250k Meta-Anchor**: `3.042099` (`RMSLE = 1.744161`)
* **Joint Optimized MSE на 250k Meta-Anchor**: `3.036010` (`RMSLE = 1.742415`)
* **Дельта RMSLE на 250k валидации**: **`-0.001746`** (уверенное улучшение).

### Скорректированные совместные метавеса (13 параметров):
- `react_stack_weights`: `[0.0888, 0.5756, 0.0000, 0.3356]`
- `churn_stack_weights`: `[0.2896, 0.2396, 0.0000, 0.4708]`
- `amount_ridge_coefficients`: `[0.1995, 0.3671, 0.0000, 0.4310]`
- `amount_ridge_intercept`: `-0.0366`
- `ALPHA`: зафиксирован на `1.1`

---

## 3. Сравнение статистик сабмитов

| Параметр | Ошибочный сабмит (v2 buggy) | Предыдущий лучший (LB 1.664078) | **Скорректированный 250k сабмит** |
| :--- | :--- | :--- | :--- |
| **Public LB RMSLE** | `2.316567` | `1.664078` | **Готов к отправке** |
| **Количество строк** | 250 000 | 250 000 | **250 000** |
| **Пропуски / NaN** | 0 / 0 | 0 / 0 | **0 / 0** |
| **Минимальный GMV** | 0.0000 руб | 0.0000 руб | **0.1654 руб** |
| **Медианный GMV** | 0.0450 руб | 7.6492 руб | **5.8033 руб** |
| **Средний GMV** | 192.80 руб | 43.21 руб | **37.72 руб** |
| **Максимальный GMV** | 403 750.34 руб | 194 230 руб | **2 825.36 руб** (надежная защита от выбросов) |
| **Корреляция с лучшим сабмитом** | 0.601 | 1.000 | **0.994094** |

---

## 4. Итоговые файлы

1. **Финальный исправленный сабмит**:
   [`submission_specialized_hurdle_joint_250k_v2.csv`](file:///c:/Users/egorg/Documents/OZON_ECUP/submission_specialized_hurdle_joint_250k_v2.csv)
2. **Банк сырых тестовых предсказаний (250k)**:
   [`test_specialists_raw_predictions_250k_v2.parquet`](file:///c:/Users/egorg/Documents/OZON_ECUP/test_specialists_raw_predictions_250k_v2.parquet)
3. **Мета-пакет**:
   [`joint_meta_weights_250k.json`](file:///c:/Users/egorg/Documents/OZON_ECUP/joint_meta_weights_250k.json)
