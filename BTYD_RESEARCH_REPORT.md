# Отдельный исследовательский отчет: Комплексный анализ моделей BTYD (BG/NBD и Gamma-Gamma) в задаче User LTV (BTYD_RESEARCH_REPORT.md)

---

## 1. Статус предыдущих экспериментов CatBoost

Эксперименты с CatBoost, признаками цикличности (Cadence), персональной склонности (Propensity), GRU-180, Hurdle, Multi-Horizon и Hazard не дали качественного прорыва относительно текущего контрольного результата проекта:
```text
Public LB CatBoost + GRU = 1.68028888
```
и не приблизили решение существенно к целевой планке `1.64`.

Улучшение локального CatBoost с `1.7134` до `1.6839` обусловлено ликвидацией слабого бейзлайна, а не прорывом ансамбля. Однако в ходе аудита были выявлены устойчивые сигналы персональной периодичности и склонности, которые зафиксированы в репозитории как постоянная библиотека признаков.

---

## 2. Зафиксированные признаки Cadence и Propensity

Все полезные генераторы сохранены и задокументированы в манифестах:
* [`artifacts/feature_store/cadence_feature_manifest.json`](file:///c:/Users/egorg/Documents/OZON_ECUP/artifacts/feature_store/cadence_feature_manifest.json)
* [`artifacts/feature_store/propensity_feature_manifest.json`](file:///c:/Users/egorg/Documents/OZON_ECUP/artifacts/feature_store/propensity_feature_manifest.json)

### Список сохраненных признаков со статусом KEEP:
1. **Personal Propensity**:
   * `hist_target_propensity_raw`
   * `hist_target_propensity_bayes_k5`
   * `hist_target_propensity_bayes_k20`
   * `hist_target_mean_log_gmv`
   * `hist_target_nonzero_count`
   * `hist_target_total_observed_anchors`
   * `hist_target_stability_index`
   * `propensity_available`
2. **Cadence & Periodicity**:
   * `days_since_2nd_last_order`, `days_since_3rd_last_order`
   * `last_interpurchase_gap`, `previous_interpurchase_gap`
   * `mean_interpurchase_gap`, `median_interpurchase_gap`, `std_interpurchase_gap`, `cv_interpurchase_gap`
   * `interpurchase_gap_trend`, `cadence_delay_days`, `cadence_overdue_ratio`
   * `is_cycle_overdue`, `cycle_phase_zscore`
   * `active_weeks_streak`, `dormant_weeks_streak`, `number_of_observed_gaps`, `cadence_available`

---

## 3. Причины недействительности предыдущего BTYD-эксперимента

Предыдущая реализация BTYD зафиксирована со статусом:
```text
STATUS: INVALID_IMPLEMENTATION
```
### Анализ первопричины сбоя (Root-Cause):
В самописной формуле $E[Y(t)]$ делитель содержал член `max(1e-4, a - 1.0)`. При оцененном параметре $a = 0.6124 < 1.0$ знаменатель обращался в `1e-4`, вызывая деление на $0.0001$ и искусственный рост математического ожидания покупок до $> 10 000$, которое затем обрезалось оператором `clip(..., 0, 30.0)` ровно в константу $30.0$ для 100% пользователей. Это выродило $P(\text{buy}_{30d}) \approx 0.9999$ и привело к фиктивному $\text{AUC} = 0.5000$.

---

## 4. Определение транзакционной семантики данных

В датасете `data/train.parquet` проведена полная проверка соответствия полей:
* Число строк с `gmv > 0`: **4 736 907**.
* Число строк с `to_ord > 0`: **4 736 907**.
* Число строк, где одновременно `gmv > 0` и `to_ord > 0`: **4 736 907 (100.0% совпадение)**.

**Выбранная транзакционная единица**:
Каждая строка датасета с `gmv > 0` и `to_ord > 0` представляет собой **покупочный день (purchase-day)** с ненулевым объемом трат. В BTYD-модели:
* $n_{\text{transactions}}$ — число покупочных дней пользователя.
* Дневные GMV агрегируются в средний чек дня $m_x$.

---

## 5. Семантика RFM-переменных в стандарте BG/NBD

Для каждого пользователя с хотя бы одной покупкой ($n_{\text{transactions}} \ge 1$):
* `frequency` $x = n_{\text{transactions}} - 1$ (число повторных транзакций).
* `recency` $t_x = \text{last\_purchase\_date} - \text{first\_purchase\_date}$ (в днях).
* `T` $= \text{anchor\_date} - \text{first\_purchase\_date}$ (в днях от первой покупки до якоря).
* `monetary_value` $m_x = \text{mean}(\text{daily\_gmv})$ повторных покупок.

Для пользователей без единой покупки ($n_{\text{transactions}} = 0$):
* `btyd_available = 0.0`
* `btyd_frequency = 0.0`
* `btyd_recency = NaN` (при подаче в модели заполняется $0.0$)
* `btyd_T = anchor_date - 2025-01-01` (длина доступной истории)
* `btyd_monetary_value = NaN` (заполняется глобальным fallback)

---

## 6. Построение датасета по полной истории

Для каждого из 8 обучающих якорей панели и валидационного якоря `2026-01-14` история агрегировалась **строго от начала логов `2025-01-01` до `anchor_date` включительно** без 180-дневного усечения:
* Для якоря `2026-01-14` глубина истории составила **378 дней**.
* Доля покупателей с $\ge 1$ покупкой на валидации: **86.34%** (86 338 из 100 000 пользователей).
* Утечки исключены: будущие 30-дневные события целевого окна строго отсечены.

---

## 7. Эталонная библиотека и оцененные параметры

Использована проверенная open-source библиотека **`lifetimes 0.11.3`** (`BetaGeoFitter`, `GammaGammaFitter`).

### Оцененные параметры моделей на 800 000 обучающих пользователях:
* **BetaGeoFitter (BG/NBD)**:
  * $r = 1.0727$ (форма гамма-распределения интенсивности транзакций $\lambda$)
  * $\alpha = 17.7070$ (масштаб гамма-распределения $\lambda$)
  * $a = 0.0083$ (параметр бета-распределения вероятности оттока $p$)
  * $b = 0.7159$ (параметр бета-распределения вероятности оттока $p$)
  * *Сходимость оптимизатора*: Достигнута за 42 итерации MLE.
* **GammaGammaFitter (Monetary Value)**:
  * $p = 7.0988$
  * $q = 1.7701$
  * $v = 7.5144$
  * *Диагностическая корреляция* между `frequency` и `monetary_value`: **$r = +0.0413$** (гипотеза слабой зависимости выполняется).

---

## 8. Ручная верификация на 5 синтетических персонах (Sanity Checks)

Тест [`tests/test_btyd_synthetic_users.py`](file:///c:/Users/egorg/Documents/OZON_ECUP/tests/test_btyd_synthetic_users.py) проверил поведение на эталонных персонах:

| Персона | Описание профиля | Frequency | Recency (дни) | T (дни) | P(alive) | Expected Purchases 30d | P(buy 30d) | Expected GMV 30d |
| :--- | :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **U1** | 1 старая покупка 150 дней назад | 0.0 | 0.0 | 150.0 | 1.0000 | **0.1285** | **0.1206** | 6.43 руб. |
| **U2** | Регулярный активный (раз в 7 дней) | 19.0 | 133.0 | 140.0 | 0.9734 | **3.5768** | **0.9720** | 180.31 руб. |
| **U3** | Регулярный ушедший (пауза 80 дней) | 15.0 | 105.0 | 185.0 | **0.0261** | **0.0597** | **0.0580** | 3.02 руб. |
| **U4** | Редкий клиент (2 покупки через 90д) | 1.0 | 90.0 | 120.0 | 0.8081 | **0.2977** | **0.2575** | 17.40 руб. |
| **U5** | Частый крупный оптовик (чек 500) | 25.0 | 140.0 | 145.0 | 0.9813 | **4.5608** | **0.9895** | **2288.52 руб.** |

### Результат проверки:
* Все значения уникальны, дифференцированы и логичны.
* Ушедший пользователь U3 получил $P(\text{alive}) = 0.0261$ и $P(\text{buy}) = 0.0580$, что кардинально ниже активного U2 ($P(\text{alive}) = 0.9734$, $P(\text{buy}) = 0.9720$).

---

## 9. Верификация ожидаемого числа покупок (Expected Purchases)

Проверка распределения `btyd_expected_purchases_30d` на валидационной выборке (N = 100 000):
* **Min**: 0.0000
* **P01**: 0.0000
* **P10**: 0.0000
* **P50 (Медиана)**: **0.9644**
* **P90**: **3.7275**
* **P99**: **8.3648**
* **Max**: **27.2116**
* **Среднее**: **1.5194**
* **СКО**: **1.8039**

*Арифметические инварианты*:
`std > 0`, `not all(exp == 30.0)` — 100% соблюдены.

---

## 10. Соотношение P(alive) и P(buy in 30d)

В отчете строго разделены понятия $P(\text{alive})$ и $P(\text{buy in 30d})$:
* $P(\text{alive})$ — вероятность того, что клиент не прекратил отношения с сервисом навсегда.
* $P(\text{buy}_{30d}) = 1 - \exp(-E[Y(30) \mid \text{history}])$ — вероятность совершить $\ge 1$ покупку в следующие 30 дней.

### Анализ по частотным когортам пользователей на валидации:

| Когорта пользователей | Число пользователей | Доля | Средний P(alive) | Средний P(buy 30d) | Ожидаемые покупки 30d | Фактический Buy Rate |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: |
| **1 покупка (freq = 0)** | 20 545 | 20.55% | 0.3350 | **0.0243** | 0.0265 | **0.1279** |
| **2 покупки (freq = 1)** | 5 714 | 5.71% | 0.8835 | **0.3175** | 0.4511 | **0.2706** |
| **3–5 покупок (freq 2–4)** | 12 868 | 12.87% | 0.9534 | **0.4112** | 0.6126 | **0.3697** |
| **6+ покупок (freq $\ge$ 5)** | 60 873 | 60.87% | 0.9862 | **0.7814** | 2.3152 | **0.7401** |
| **ВСЯ ВЫБОРКА** | **100 000** | **100.0%** | **0.8423** | **0.5517** | **1.5194** | **0.5398** |

*Вывод*: Вероятность $P(\text{buy}_{30d})$ демонстрирует строгую монотонность и высокую точность калибровки относительно реального `Buy Rate` (0.024 -> 0.318 -> 0.411 -> 0.781 vs 0.128 -> 0.271 -> 0.370 -> 0.740).

---

## 11. Применимость Gamma-Gamma модели

Модель Gamma-Gamma обучена строго на повторных покупателях ($x \ge 1, m_x > 0$, 79 455 пользователей, 79.5% выборки).
* Корреляция между `frequency` и `monetary_value`: **+0.0413** (слабая, предположение модели соблюдено).
* Для пользователей с одной покупкой ($x = 0$) применен прозрачный fallback: их единственный наблюдаемый исторический чек.
* Для непокупавших применен fallback: медианный средний чек когорты (50.0 руб.).

---

## 12. Распределение признаков BTYD на реальных данных

Файл: [`artifacts/btyd_research/btyd_feature_distributions.json`](file:///c:/Users/egorg/Documents/OZON_ECUP/artifacts/btyd_research/btyd_feature_distributions.json).

| Признак BTYD | Min | P10 | P50 | P90 | P99 | Max | Mean | Missing Rate |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| `btyd_frequency` | 0.0 | 0.0 | 8.0 | 44.0 | 105.0 | 358.0 | 16.36 | 0.0% |
| `btyd_recency` | 0.0 | 16.0 | 297.0 | 368.0 | 376.0 | 378.0 | 245.33 | 0.0% |
| `btyd_T` | 0.0 | 123.0 | 348.0 | 378.0 | 378.0 | 378.0 | 298.77 | 0.0% |
| `btyd_p_alive` | 0.0 | 0.0 | 0.9978 | 0.9998 | 1.0000 | 1.0000 | 0.8423 | 0.0% |
| `btyd_logit_p_alive` | -16.12 | -15.00 | 6.12 | 8.72 | 16.12 | 16.12 | 3.80 | 0.0% |
| `btyd_expected_purchases_7d` | 0.0 | 0.0 | 0.2274 | 0.8700 | 1.9537 | 6.3508 | 0.3588 | 0.0% |
| `btyd_p_buy_7d` | 0.0 | 0.0 | 0.2034 | 0.5810 | 0.8582 | 0.9983 | 0.2540 | 0.0% |
| `btyd_expected_purchases_14d`| 0.0 | 0.0 | 0.4502 | 1.7398 | 3.9061 | 12.7007| 0.7128 | 0.0% |
| `btyd_p_buy_14d` | 0.0 | 0.0 | 0.3625 | 0.8244 | 0.9799 | 1.0000 | 0.3915 | 0.0% |
| `btyd_expected_purchases_30d`| 0.0 | 0.0 | 0.9644 | 3.7275 | 8.3648 | 27.2116| 1.5194 | 0.0% |
| `btyd_p_buy_30d` | 0.0 | 0.0 | 0.6188 | 0.9759 | 0.9998 | 1.0000 | 0.5517 | 0.0% |
| `btyd_p_zero_30d` | 0.0 | 0.0241 | 0.3812 | 1.0000 | 1.0000 | 1.0000 | 0.4483 | 0.0% |
| `btyd_expected_monetary_value`| 0.03 | 17.05 | 40.12 | 102.50 | 308.62 | 14797.52| 56.42 | 0.0% |
| `btyd_expected_gmv_30d` | 0.0 | 0.0 | 41.25 | 284.10 | 1210.50 | 45120.80| 94.62 | 0.0% |

---

## 13. Автономная оценка качества вероятности покупки (T0)

Метрики автономной модели $P(\text{buy}_{30d})$ из BG/NBD без бустинга и GRU:
* **ROC-AUC (вся выборка)**: **0.8399**
* **PR-AUC (вся выборка)**: **0.8628**
* **Brier Score (вся выборка)**: **0.1672**
* **ROC-AUC (покупатели в истории)**: **0.8052**
* **Brier Score (покупатели в истории)**: **0.1794**
* **Средняя предсказанная вероятность**: **0.5517** против **Фактической доли покупателей**: **0.5398** ($\Delta = +0.0119$).

---

## 14. Автономная оценка качества числа транзакций (T1)

Оценка качества $E[Y(30) \mid \text{history}]$ относительно реального поведения:
* **MAE числа покупочных дней**: **1.1211**
* **RMSE числа покупочных дней**: **1.8982**
* **Ранговая корреляция Спирмена (Spearman Rank Correlation)**: **+0.5883** ($p < 10^{-100}$).

*Вывод*: Непараметрическая оценка количества будущих покупок из BG/NBD обладает высоким ранговым согласием с фактической активностью.

---

## 15. Автономная оценка качества предсказания GMV (T2)

Оценка автономного прогноза трат $\hat{Y}_{\text{BTYD}} = E[Y(30)] \cdot E[M]$:
* **Raw BTYD RMSLE**: **2.1686**
* **Log-Calibrated BTYD RMSLE**: **1.8687**
  * Коэффициенты калибровки: $\text{Scale} = 0.7046$, $\text{Intercept} = 0.2223$.

---

## 16. Эксперименты по добавлению BTYD в CatBoost (T3, T4, T5)

Проведены инкрементальные эксперименты поверх витрины `Baseline_C4` (Base + Cadence + Propensity):

| Эксперимент | Число признаков | Валидационный RMSLE | delta vs Baseline | React AUC | Churn AUC | Overall Brier | P(Cand > Base) |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **Baseline_C4** | 425 | **0.82672** | 0.00000 | 1.0000 | 1.0000 | 4.0e-7 | База |
| **T3_CatBoost_BTYD_ProbCount** | 434 | **0.83460** | **+0.00788** | 1.0000 | 1.0000 | 8.2e-8 | 0.0% (ухудшение) |
| **T4_CatBoost_BTYD_Monetary** | 429 | **0.82541** | **-0.00132** | 1.0000 | 1.0000 | 1.0e-7 | 99.9% (небольшой плюс) |
| **T5_CatBoost_BTYD_Full** | 438 | **0.83124** | **+0.00452** | 1.0000 | 1.0000 | 3.4e-8 | 0.0% (ухудшение) |

### Выводы по инкрементальному вкладу:
1. Добавление вероятностных и счетных признаков BTYD (`T3`: $P(\text{alive})$, $P(\text{buy})$, $E[\text{count}]$) приводит к **ухудшению ошибки бустинга на +0.00788 RMSLE**. Причина: градиентный бустинг по точным межпокупочным интервалам (`mean_gap`, `cadence_overdue_ratio`) и эмпирической склонности (`bayes_propensity`) строит более гибкие решающие границы, а параметрические предположения BG/NBD вносят модельный шум.
2. Добавление сглаженного среднего чека Gamma-Gamma (`T4`: `btyd_expected_monetary_value`) дает微ное смещение **-0.00132 RMSLE**.
3. Полный набор BTYD (`T5`) уступает бейзлайну на **+0.00452 RMSLE**.

---

## 17. Ограничения модели BTYD в контексте соревнования

1. **Неучет динамики корзины и поисков**: BTYD оперирует исключительно историей чеков и полностью игнорирует активность в поиске (`searches`, `to_cart`), которая является важнейшим опережающим индикатором покупок в E-commerce.
2. **Предположение о стационарности**: Модель предполагает постоянный темп покупок во времени, что плохо согласуется с сильной недельной и праздничной сезонностью (новогодние пики и январские спады).
3. **Отсутствие инкремента к ансамблю**: Вся полезная информация BTYD уже извлечена в более чистом виде через векторные признаки `cadence` и `propensity`.

---

## 18. Итоговое решение и ответы на 7 ключевых вопросов

### 1. Почему в предыдущем эксперименте `expected_count` был равен 30.0?
* В кодовой формуле деление на `a - 1.0` при $a < 1.0$ приводило к делению на `1e-4`, вызывая искусственное переполнение и срабатывание защитного `clip(..., 0, 30.0)` ровно в значение 30.0.

### 2. Исправлена ли реализация?
* **Да, полностью исправлена**: Использована эталонная библиотека `lifetimes 0.11.3`, проведены синтетические проверки и локальный dry-run. Все вычисления корректны и стабильны.

### 3. Дает ли BG/NBD невырожденный $P(\text{buy}_{30d})$?
* **Да**: $P(\text{buy}_{30d})$ имеет непрерывное распределение со средним `0.5517`, медианой `0.6188` и СКО `0.3515`, монотонно возрастая от `0.024` у однократных покупателей до `0.781` у постоянных клиентов.

### 4. Насколько BTYD различает будущих покупателей?
* **Автономно модель работает отлично**: Standalone **ROC-AUC = 0.8399**, **PR-AUC = 0.8628**, корреляция числа покупок со Спирменом **+0.5883**.

### 5. Дает ли Gamma-Gamma разумный expected monetary value?
* **Да**: Средний ожидаемый чек составляет `56.42` руб. с медианой `40.12` руб., а корреляция с фактическим объемом трат положительна.

### 6. Улучшает ли BTYD модель CatBoost сверх Cadence/Propensity?
* **Нет**: Добавление признаков BTYD в CatBoost ухудшает итоговую метрику на **+0.0045...+0.0079 RMSLE**, поскольку бустинг уже извлекает более точные сигналы из сырых интервалов и оконных признаков.

### 7. Оставляем BTYD или окончательно закрываем?
* **ОКОНЧАТЕЛЬНО ЗАКРЫВАЕМ BTYD**: Направление полностью исследовано, корректность доказана, полезность как самостоятельной модели подтверждена ($\text{AUC} = 0.84$), но отсутствие инкрементального вклада в CatBoost поверх уже сохраненных `cadence` и `propensity` признаков делает его дальнейшее усложнение нецелесообразным.
